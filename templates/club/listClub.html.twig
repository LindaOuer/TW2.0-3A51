{% for c in clubs %}
  {{c.nom}}
  |
  <a href="{{ path('clubDetails', {id: c.id}) }}">details
  </a>
  |
  <a href="{{ path('clubDelete', {id: c.id}) }}">delete
  </a>
  <br>
  <a href="{{ path('update_club', {id: c.id}) }}">delete
  </a>
{% endfor %}
